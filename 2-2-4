CREATE DATABASE MDL2_2_4;

USE MDL2_2_4;

CREATE TABLE users (
    user_id INT PRIMARY KEY,
    username VARCHAR(50) UNIQUE,
    user_password VARCHAR(255) NOT NULL,
    user_status VARCHAR(20) DEFAULT 'ACTIVE' CHECK (user_status IN ('ACTIVE', 'INACTIVE'))
);
